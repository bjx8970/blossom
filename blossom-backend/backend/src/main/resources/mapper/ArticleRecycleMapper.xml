<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blossom.backend.server.article.recycle.ArticleRecycleMapper">
    <!-- 文章保存进回收站 -->
    <insert id="save">
        insert into blossom_article_recycle (
            id, pid, name, icon, tags, sort, cover, describes, pv, uv, likes, words,
            version, revision, color, markdown, html, toc, cre_time, upd_time, del_time, user_id
        ) (
            select id,
                   pid,
                   name,
                   icon,
                   tags,
                   sort,
                   cover,
                   describes,
                   pv,
                   uv,
                   likes,
                   words,
                   version,
                   revision,
                   color,
                   markdown,
                   html,
                   toc,
                   cre_time,
                   cre_time,
                   now(),
                   user_id
              from blossom_article
             where id = #{id} and user_id = #{userId}
        );
    </insert>

    <select id="listAll" resultType="com.blossom.backend.server.article.recycle.pojo.ArticleRecycleEntity">
        select id,
               name,
               del_time
          from blossom_article_recycle
         where user_id = #{userId}
    </select>

    <!--  -->
    <insert id="restore">
        INSERT INTO blossom_article (
            id, pid, name, icon, tags, sort, cover, describes,
            star_status, open_status, open_version, pv, uv, likes, words,
            version, revision, color, toc, markdown, html, cre_time, upd_time,
            user_id, upd_markdown_time
        ) (
            select id,
                   #{pid},
                   name,
                   icon,
                   tags,
                   sort,
                   cover,
                   describes,
                   0,
                   0,
                   0,
                   pv,
                   uv,
                   likes,
                   words,
                   version,
                   revision,
                   color,
                   toc,
                   markdown,
                   html,
                   cre_time,
                   now(),
                   user_id,
                   now()
              from blossom_article_recycle
             where id = #{id} and user_id = #{userId})
    </insert>

    <delete id="delByUserId">
        delete from blossom_article_recycle where user_id = #{userId}
    </delete>
</mapper>
